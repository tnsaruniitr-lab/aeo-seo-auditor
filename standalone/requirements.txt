fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
anthropic>=0.40.0
python-dotenv>=1.0.0

# Agent-mode dependencies (parity layer)
httpx>=0.27.0          # web_fetch, web_search (Tavily), persist_audit
selectolax>=0.3.21     # fast HTML parser for web_fetch digest
playwright>=1.45.0     # render_page_js — JS rendering + perf metrics
